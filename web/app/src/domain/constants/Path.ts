const INDEX = '/';
const REGISTER = '/register';
const LOGIN = '/login';
const FORBIDDEN = '/forbidden';
const TEMP = '/temp';
const WALKER = '/walker';

export default Object.freeze({
  INDEX,
  REGISTER,
  LOGIN,
  FORBIDDEN,
  TEMP,
  WALKER
});
