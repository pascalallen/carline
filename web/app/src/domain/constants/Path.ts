const INDEX = '/';
const LOGIN = '/login';
const FORBIDDEN = '/forbidden';
const TEMP = '/temp';
const WALKER = '/walker';

export default Object.freeze({
  INDEX,
  LOGIN,
  FORBIDDEN,
  TEMP,
  WALKER
});
